<?php

// array which has regex as the key, and the class and method in value

$routes = array (
                    '/^\/api\/getMessages.json/' => array( 'controller' => 'Controller_Messages', 'method' => 'getMessages' ), 

                );
